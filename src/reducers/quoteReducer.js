import { GET_CATEGORY, GET_QUOTE } from '../actions/types';

const initialState = {
  categories: [],
  quote: {}
};

export default function (state = initialState, action) {
  switch (key) {
    case value:

      break;

    default:
      break;
  }
}